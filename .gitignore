# XcodeGen-generated project and Info.plist — regenerate with `xcodegen generate`
imarp.xcodeproj/
imarp/Resources/Info.plist

# Build products
build/
DerivedData/

# Xcode user-specific
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# marp-engine-build: npm deps (reproducible via `npm install`)
marp-engine-build/node_modules/
