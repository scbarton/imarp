# XcodeGen-generated project and Info.plist — regenerate with `xcodegen generate`
imarp.xcodeproj/
imarp/Resources/Info.plist

# Build products
build/
DerivedData/

# Xcode user-specific
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# marp-engine-build: npm deps (reproducible via `npm install`)
marp-engine-build/node_modules/

# Scratch output from shell/theme extraction experiments
marp-engine-build/assembled_theme_test.html

# SourceOnlyTest.marpbundle is deliberately source-only, to exercise
# on-device rendering — never commit the cache it regenerates.
deck-source/SourceOnlyTest.marpbundle/source.html
